#include <stdio.h>

int main() {

    int N, M, P, i, j;

    scanf("%d", &N);

    for(i = 0; i < N; i++){
        scanf("%d", &M);
        for(j = 0; j < M; j++){
            scanf("%d", &P);
        }
    }

    return 0;
}
